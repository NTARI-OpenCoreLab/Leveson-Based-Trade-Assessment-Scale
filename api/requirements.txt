# LBTAS API dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.20.0

# Run the app from the REPO ROOT, so the `api` package resolves for its
# relative imports:
#   uvicorn api.main:app --reload
#
# Run the store/validation tests from INSIDE this directory (bare imports):
#   cd api && python3 test_rating_validation.py
#   cd api && python3 test_event_store.py
#
# test_main.py drives the actual HTTP layer (real uvicorn subprocess, temp
# DB, stdlib urllib — no extra dependency). Run from the REPO ROOT instead:
#   python3 api/test_main.py
#
# Reads/dismiss/uphold/apply-timeouts require LBTAS_API_KEY; submissions
# (POST /ratings, /contest, /exchanges) require a SEPARATE LBTAS_SUBMIT_KEY —
# CLAUDE.md treats them as distinct capabilities. Both fail closed if unset:
#   LBTAS_API_KEY=<read-key> LBTAS_SUBMIT_KEY=<submit-key> uvicorn api.main:app --reload
#   curl -H "X-API-Key: <read-key>" http://localhost:8000/ratings/<party>/<role>
#   curl -X POST -H "X-API-Key: <submit-key>" http://localhost:8000/ratings -d '...'
